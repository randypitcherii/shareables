# =============================================================================
# Hive Table Experiments - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# These can also be set from terraform outputs:
#   export HIVE_EVAL_BUCKET_EAST_1A=$(cd terraform && terraform output -raw bucket_east_1a)

# -----------------------------------------------------------------------------
# S3 Bucket Configuration
# -----------------------------------------------------------------------------
HIVE_EVAL_BUCKET_EAST_1A=your-bucket-east-1a
HIVE_EVAL_BUCKET_EAST_1B=your-bucket-east-1b
HIVE_EVAL_BUCKET_WEST_2=your-bucket-west-2

# -----------------------------------------------------------------------------
# AWS Glue Configuration
# -----------------------------------------------------------------------------
HIVE_EVAL_GLUE_DATABASE=your_glue_database
HIVE_EVAL_GLUE_CATALOG=your_glue_federation_catalog

# -----------------------------------------------------------------------------
# Databricks Unity Catalog Configuration
# -----------------------------------------------------------------------------
HIVE_EVAL_UC_CATALOG=your_uc_catalog
HIVE_EVAL_UC_SCHEMA=your_uc_schema

# -----------------------------------------------------------------------------
# Databricks Serverless (optional)
# -----------------------------------------------------------------------------
HIVE_EVAL_SERVERLESS_WAREHOUSE_ID=your_warehouse_id
